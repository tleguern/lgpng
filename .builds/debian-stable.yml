---
image: debian/stable
packages:
  - gcc
  - zlib1g-dev
  - bmake
sources:
  - https://git.sr.ht/~tleguern/lgpng
tasks:
  - setup: |
      cd lgpng
      ./configure
  - build: |
      cd lgpng
      bmake
  - regress: |
      cd lgpng
      bmake regress
